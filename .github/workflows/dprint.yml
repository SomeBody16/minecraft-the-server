name: dprint

on:
  pull_request:

jobs:
  check:
    name: dprint check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check formatting
        uses: dprint/check@v2.3
