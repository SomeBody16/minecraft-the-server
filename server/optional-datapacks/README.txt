To use any of the optional datapacks, move them to config/openloader/data.

In the case of a datapack that uses Vault Hunters API (VHAPI), make sure that server config sync is turned ON in the config (it should be by default).